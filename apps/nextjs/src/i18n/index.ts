// Re-export locale configuration for use across the app
// These exports are safe to use in both client and server components
export { locales, localeNames, defaultLocale } from "./config";
export type { Locale } from "./config";
